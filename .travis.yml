language: groovy
jdk:
 - oraclejdk8
 - oraclejdk7
 - openjdk7
branches:
 only:
 - master

before_script:
 - chmod +x grailsw

script: ./grailsw refresh-dependencies
 && ./grailsw "test-app -integration"